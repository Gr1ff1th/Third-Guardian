using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActionButtons : Interactable
{

    public ABCanvas abCanvas;
    public override void Interact()
    {
        //GameManager.ins.abCanvas.Activate();
        //ABCanvas CheckThis = GameManager.ins.currentNode.GetComponentInChildren<ABCanvas>();
        //abCanvas.Activate();
        //gameObject.transform.position = new Vector3(Input.mousePosition.x, Input.mousePosition.y, Input.mousePosition.z);
    }
}
